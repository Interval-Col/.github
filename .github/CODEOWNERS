# CODEOWNERS for Interval-Col/.github
#
# This is the org's meta-repo: the branching/deploy policy, engineering
# standards, agent instructions, shared templates, brand assets, and the
# rollout tracker live here. Changes to these set the rules the rest of
# the org follows.
#
# Gatekeeper model: @gczuluaga is the SOLE code owner for this repo.
# Combined with branch protection (require code-owner review · 1 approval ·
# admins not enforced), this means EVERY PR — including @SKuger01's and any
# other contributor's — must be approved by @gczuluaga before it can merge,
# while @gczuluaga lands their own PRs via admin (GitHub forbids approving
# your own PR). @gczuluaga is gatekeeper + admin here.
#
# (CODEOWNERS is per-repo — it is NOT inherited org-wide the way the
# default PR template is. This file governs PRs to THIS repo only.)

# Everything in this repo is gatekept by @gczuluaga.
*                               @gczuluaga
