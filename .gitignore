# macOS
.DS_Store
.AppleDouble
.LSOverride

# macOS custom-folder icon resource file (literally "Icon" + a CR → 5 chars).
# Use Icon? (not bare "Icon") so it does NOT swallow legit icon/ dirs or Icon.vue
# on a case-insensitive filesystem.
Icon?

# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share, NFS, SMB, or WebDAV
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Worktrees y estado local de agentes de Claude — nunca se versionan.
# Medido 2026-09-03: en public-web esto eran 401 MB y 17.902 archivos untracked,
# que un `git add -A` se habría tragado enteros.
.claude/
