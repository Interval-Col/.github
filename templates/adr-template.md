# ADR NNNN — <Title> (YYYY-MM-DD)

> Lightweight, per-repo decision record. Lives in `docs/decisions/`. English.
> No YAML frontmatter — status is inline below the title.
> If the decision spans more than one repo OR sets a durable org-wide constraint
> OR is irreversible, STOP and write an RFC instead (`Interval-Col/rfcs/`), then
> link it here. See the RFC-vs-ADR threshold in `.github/KNOWLEDGE-MANAGEMENT.md`.

**Status:** proposed | accepted | rejected | deferred | superseded-by ADR NNNN
**Deciders:** @handle — decided live in session YYYY-MM-DD *(optional)*

## Context
What forced this decision — the problem, constraints, what was true before. Cite
the plan / RFC / incident that surfaced it.

## Decision
The call, stated as a decision ("We will …"). Number sub-decisions if any.

## Consequences
What it buys and what it costs — follow-on work, new constraints. Mark open human
decisions with 🛑.

## Alternatives considered
What else was looked at and why it lost. "Do nothing" is valid.

## References
- RFC(s), plan(s), incident(s), prior/superseded ADRs, the PR(s) that carried it out.

> Filename: `docs/decisions/NNNN-kebab-title-YYYY-MM-DD.md`. Scaffold with `/decision`.
